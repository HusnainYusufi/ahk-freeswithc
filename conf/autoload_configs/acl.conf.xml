<configuration name="acl.conf" description="Network Lists">
  <network-lists>
    <!--
       These ACL's are automatically created on startup.

       rfc1918.auto  - RFC1918 Space
       nat.auto      - RFC1918 Excluding your local lan.
       localnet.auto - ACL for your local lan.
       loopback.auto - ACL for your local lan.
    -->

    <list name="lan" default="allow">
      <node type="deny" cidr="192.168.42.0/24"/>
      <node type="allow" cidr="192.168.42.42/32"/>
    </list>

    <!--
      This will traverse the directory adding all users
      with the cidr= tag to this ACL, when this ACL matches
      the users variables and params apply as if they
      digest authenticated.
    -->
    <list name="domains" default="deny">
      <!-- domain= is special it scans the domain from the directory to build the ACL -->
      <node type="allow" domain="$${domain}"/>
      <!-- use cidr= if you wish to allow ip ranges to this domains acl. -->
      <!-- <node type="allow" cidr="192.168.0.0/24"/> -->
    </list>

    <!-- ESL - Trusted IPs Only -->
    <list name="esl_trusted_only" default="deny">
      <node type="allow" cidr="154.57.194.70/32"/>
      <node type="allow" cidr="128.94.9.13/32"/>    <!-- New Backend -->
      <node type="allow" cidr="10.2.2.179/32"/>     <!-- Local Backend -->
      <node type="allow" cidr="39.62.128.120/32"/>  <!-- Whitelisted Backend -->
      <node type="allow" cidr="20.174.217.63/32"/>  <!-- Whitelisted Backend (OLD) -->
      <node type="allow" cidr="20.174.157.124/32"/> <!-- CommsEngine Backend (OLD) -->
      <node type="allow" cidr="20.233.211.5/32"/>   <!-- CommsEngine Backend (CURRENT) -->
      <node type="allow" cidr="10.2.2.247/32"/>     <!-- Whitelisted Backend -->
      <node type="allow" cidr="127.0.0.1/32"/>
    </list>

    <!-- SIP - Trusted IPs Only (Laptop + Backend + Localhost) -->
    <list name="sip_trusted_only" default="deny">
      <node type="allow" cidr="154.57.194.70/32"/>  <!-- Backend / VPN egress -->
      <node type="allow" cidr="128.94.15.9/32"/>   <!-- Laptop -->
      <node type="allow" cidr="128.94.9.13/32"/>   <!-- New Backend -->
      <node type="allow" cidr="10.2.2.179/32"/>    <!-- Local Backend -->
      <node type="allow" cidr="39.62.128.120/32"/> <!-- Whitelisted Backend -->
      <node type="allow" cidr="20.174.217.63/32"/> <!-- Whitelisted Backend (OLD) -->
      <node type="allow" cidr="20.174.157.124/32"/> <!-- CommsEngine Backend (OLD) -->
      <node type="allow" cidr="20.233.211.5/32"/>   <!-- CommsEngine Backend (CURRENT) -->
      <node type="allow" cidr="10.2.2.247/32"/>    <!-- Whitelisted Backend -->
      <node type="allow" cidr="127.0.0.1/32"/>     <!-- Localhost -->
      <node type="deny" cidr="185.243.0.0/16"/>    <!-- Block attacker network -->
    </list>

  </network-lists>
</configuration>
